<view class="tabbar" wx:if="{{registered}}">
  <view
    wx:for="{{tabs}}"
    wx:key="path"
    class="tab-item {{item.path === selected ? 'active' : ''}}"
    data-path="{{item.path}}"
    bindtap="onChange"
  >
    <view class="tab-text">{{item.text}}</view>
  </view>
</view>
